<nav class="navHead">
    <ul>
        <li>
            <a href="{{ path('homepage') }}">Accueil</a>
        </li>
        <li>
            <a href="{{ path('cars_index') }}">Nos Voitures</a>
        </li>
        <li>
            <a href="{{ path('homepage') }}"><h2>Guido&Luigi</h2></a>
        </li>
        {% if app.user %}
            <li>
                <a href="{{path('account')}}">Mon compte</a>
            </li>
            <li>
                <a href="{{path('account_logout')}}">Déconnexion</a>
            </li>
        {% else %}
            <li>
                <a href="{{path('account_register')}}">Inscription</a>
            </li>
            <li>
                <a href="{{path('account_login')}}">Connexion</a>
            </li>
        {% endif %}
    </ul>
</nav>
{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{label}}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}