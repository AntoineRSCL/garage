{% extends "base.html.twig" %}

{% block title %}Inscription au site{% endblock %}

{% block body %}
    <div class="container full">
        <h1>Inscription sur Mon Garage</h1>
        {{form_start(myForm)}}
        
            {{form_widget(myForm)}}
            <button type="submit" class="btn btn-outline-success">Confirmez l'inscription</button>

        {{form_end(myForm)}}
    </div>
{% endblock %}